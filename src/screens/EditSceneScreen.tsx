// EditSceneScreen - Reuses CreateSceneScreen logic with pre-filled data
// This is identical to CreateSceneScreen but loads existing scene data

import CreateSceneScreen from './CreateSceneScreen';

export default CreateSceneScreen;  // Export the same component for now
// Navigation params will determine if it's edit or create mode
